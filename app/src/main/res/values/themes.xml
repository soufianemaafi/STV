<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.STV" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/stv_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Background colors -->
        <item name="android:colorBackground">@color/stv_dark_background</item>
        <item name="android:colorBackgroundCacheHint">@color/stv_dark_background</item>
        <item name="colorSurface">@color/stv_surface</item>
        <item name="colorOnSurface">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/stv_dark_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/stv_dark_background</item>
        <!-- Utilisation explicite de l'icône animée et statique -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo_stv</item>
        <item name="postSplashScreenTheme">@style/Theme.STV</item>
    </style>
</resources>